twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    exception_controller: null
    # pour ajouter le thème de formulaire de bootstrap
    form-themes: ['bootstrap_4_layout.html.twig']
